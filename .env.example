# Divan — model & backend config (CLAUDE.md §2). String'leri koda hardcode etme.

# --- Claude (Claude Code interaktif — login auth, API key DEĞİL) ---
CLAUDE_CODE_BIN=claude
MODEL_ATHENA=claude-opus-4-8
MODEL_THEMIS=claude-opus-4-8

# --- GPT (Codex exec — login auth) ---
CODEX_BIN=codex
MODEL_SOCRATES=gpt-5.5

# --- Gemini (agy — login auth) ---
AGY_BIN=agy
MODEL_APOLLO=gemini-3.5-flash
# karmaşık davalarda: gemini-3.1-pro

# --- API tarafı (key gerekli — henüz yok, placeholder) ---
DEEPSEEK_API_KEY=
DEEPSEEK_API_BASE=https://api.deepseek.com/chat/completions
MODEL_HEPHAESTUS=deepseek-v4-flash
XAI_API_KEY=
XAI_API_BASE=https://api.x.ai/v1/chat/completions
MODEL_ATLAS=grok-4.1-fast

# --- Adapter davranışı ---
DIVAN_PTY_TIMEOUT=120
DIVAN_CLI_TIMEOUT=300
DIVAN_API_TIMEOUT=120
